name: Validate PR 25 changes

on:
  push:
    branches:
      - agent/importar-hospedin-relatorio-agua

permissions:
  contents: write

concurrency:
  group: pr25-${{ github.ref_name }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: agent/importar-hospedin-relatorio-agua
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Apply idempotent build fixes
        run: node scripts/fix-pr25-build.mjs

      - name: Apply HotelAI interface updates
        run: node scripts/apply-reservation-flow-updates.mjs

      - name: Install dependencies
        run: npm ci

      - name: Build and regenerate routes
        run: npm run build

      - name: Typecheck
        run: npm run typecheck

      - name: Commit generated corrections
        env:
          TARGET_BRANCH: agent/importar-hospedin-relatorio-agua
        run: |
          git add src/components/MapaQuartos.tsx src/routes/_authenticated/assistente.tsx
          if [ -f src/routeTree.gen.ts ]; then
            git add -f src/routeTree.gen.ts
          fi
          if git diff --cached --quiet; then
            echo "No generated changes to commit"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "finalizar HotelAI e regenerar rotas"
          git fetch origin "$TARGET_BRANCH"
          git rebase "origin/$TARGET_BRANCH"
          git push origin "HEAD:$TARGET_BRANCH"
